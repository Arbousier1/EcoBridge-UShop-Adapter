name: Java CI with Gradle (Paper 1.21.1)

# 定义触发条件
on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
<<<<<<< HEAD
    # 1. 拉取代码并开启 LFS 支持
    # 必须开启 lfs: true，否则下载到的 JAR 包仅为 1KB 的指针文件，会导致编译失败
=======
    # 1. 拉取代码并强制开启 LFS
    # 必须开启 lfs: true，否则 libs 文件夹下的 JAR 包只是文本指针，会导致编译报错
>>>>>>> bbdda28 (xiufu)
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        lfs: true

<<<<<<< HEAD
    # 2. 配置 JDK 21 环境
=======
    # 2. 配置 Java 21 环境
>>>>>>> bbdda28 (xiufu)
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: gradle # 开启 Gradle 缓存，加速后续构建

<<<<<<< HEAD
    # 3. 精准安装本地依赖库
    # 使用 find 自动探测嵌套目录中的 JAR
    # 使用 -DgeneratePom=true 强制生成本地 POM，跳过开发者原有的 Parent POM 校验错误
    - name: Install Local Dependencies
      run: |
        # 安装 UltimateShop 4.2.3
        ULTIMATE_PATH=$(find libs -name "UltimateShop-4.2.3.jar" | head -n 1)
        echo "定位到 UltimateShop: $ULTIMATE_PATH"
        mvn install:install-file \
          -Dfile="$ULTIMATE_PATH" \
          -DgroupId=cn.superiormc \
          -DartifactId=UltimateShop \
          -Dversion=4.2.3 \
          -Dpackaging=jar \
          -DgeneratePom=true

        # 安装 EcoBridge 1.0 (坐标需与 pom.xml 严格对齐)
        ECO_PATH=$(find libs -name "EcoBridge-1.0.jar" | head -n 1)
        echo "定位到 EcoBridge: $ECO_PATH"
        mvn install:install-file \
          -Dfile="$ECO_PATH" \
          -DgroupId=top.ellan.ecobridge \
          -DartifactId=EcoBridge \
          -Dversion=1.0 \
          -Dpackaging=jar \
          -DgeneratePom=true

    # 4. 执行 Maven 构建与 ProGuard 混淆
    # 使用 -U 参数强制刷新依赖索引，确保识别到刚刚 install 的本地包
    - name: Build and Obfuscate
      run: mvn clean package -U --file pom.xml

    # 5. 上传构建产物
    # 仅上传经过 ProGuard 混淆保护的 -protected.jar 文件
    - name: Upload Protected Artifact
      uses: actions/upload-artifact@v4
      with:
        name: EcoBridgeMiddleware-Release
        path: target/*-protected.jar
        if-no-files-found: error
=======
    # 3. 授予 Gradle 执行权限
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    # 4. 执行构建任务
    # 我们直接运行 proguard 任务，它会自动触发 shadowJar 任务
    - name: Build and Obfuscate with Gradle
      run: ./gradlew proguard --no-daemon

    # 5. 上传混淆后的保护包
    - name: Upload Protected Artifact
      uses: actions/upload-artifact@v4
      with:
        name: EcoBridgeMiddleware-Gradle-Release
        # Gradle 默认产物路径在 build/libs/
        path: build/libs/*-protected.jar
        if-no-files-found: error
>>>>>>> bbdda28 (xiufu)
